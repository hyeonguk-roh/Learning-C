#include <stdio.h>
#include <stdlib.h>

int main()
{
    int lucky_numbers[] = {4, 8, 15, 16, 23, 42}; // [] means array or storing multiple pieces of information
    int number[10];                               // an array of size 10
    return 0;
}